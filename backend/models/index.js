const User = require('./User');
const Profile = require('./Profile');
const Claim = require('./Claim');
const Policy = require('./Policy');
const Comment = require('./Comment');

module.exports = { User, Profile, Claim, Policy, Comment };
